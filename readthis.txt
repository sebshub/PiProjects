this is from the pi
you feel me
I sure do and I'm now updating it on git.

So the Commands you used to do this are 
git add file name
git commit -m “Add Readme.txt”
git push

Use get status to see if a pull is needed.

Use get pull to pull down updates from host.
